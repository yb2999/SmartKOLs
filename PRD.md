# SmartKOLs 产品需求文档（PRD）

**版本**：v0.3
**日期**：2026-04-10
**状态**：Demo 阶段（前端原型完成）

---

## 1. 产品概述

SmartKOLs 是一款面向 Twitter 的 AI KOL 矩阵管理工具，提供网页版操作看板。用户可批量管理数十至数百个 Twitter 账号，为每个账号配置独立的 AI 人格、信息源与自动发帖策略，并通过自动互动（关注、转发、评论、回复）让账号看起来像真人，而不是只会发推的机器。

当前版本为前端 Demo，全部数据通过 Mock Store + localStorage 伪持久化驱动（200 个预置账号），用于验证产品逻辑与交互流程。

---

## 2. 目标用户

| 用户类型 | 描述 |
|---|---|
| 自用场景 | 构建 KOL 矩阵，用于项目推广、话题引导 |
| 商业客户 | KOL 经纪人或机构，需批量管理旗下多个 Twitter 账号 |

---

## 3. 信息架构与导航

系统采用左侧导航 + 主内容区布局，共 **6 个一级入口**：

| 模块 | 路由 | 说明 |
|---|---|---|
| 概览 | `/dashboard` | 运营数据看板 + 热门话题 |
| 账号管理 | `/accounts` | 账号列表、分组、批量操作、健康评分 |
| 内容日历 | `/calendar` | 整个矩阵的发帖排期周视图 |
| 内容审核 | `/drafts` | AI 草稿待批准队列 |
| 监控中心 | `/monitoring` | 私信监控 + 分类 + 报警 |
| 设置 | `/settings` | AI 配置、团队成员、账户管理 |

此外还有：
- **登录页** `/login` —— 首页门面
- **全局搜索** Cmd+K 命令面板（任意页面可用）
- **通知中心** 侧边栏铃铛入口

账号详情页位于 `/accounts/[id]`，包含 **6 个子标签**：

| 子页 | 路由 | 说明 |
|---|---|---|
| 人格配置 | `/persona` | AI 人格设定 + 健康分卡片 |
| 信息源 | `/sources` | 内容来源监控配置 |
| 自动发帖 | `/autopost` | 发帖时间、频率、风格 |
| **互动自动化** | `/engagement` | 自动关注/转发/评论/回复 + 互动日志 |
| 推文预览 | `/preview` | AI 生成推文预览 |
| **数据分析** | `/analytics` | 粉丝增长、互动统计、热力图 |

---

## 4. 核心功能

### 4.1 概览看板（Dashboard）

**6 个统计卡片**：总账号数 · 总粉丝数 · 待审核草稿 · 今日互动 · 未读消息 · 账号分组。

**热门话题面板**：展示近 24 小时 10 个热门话题（分"加密/AI/财经"等类别，带热度数值）。点击任一话题 → 一键为矩阵批量生成相关推文草稿 → 跳转到内容审核页。

**账号列表预览** + **最新消息预览**：Dashboard 下方并排显示前 5 项。

### 4.2 账号管理

#### 4.2.1 账号列表
- 支持添加单个账号、批量导入
- 表格视图：账号信息、分组、运行状态、**健康分**、粉丝数、推文数、管理入口
- 搜索与分组侧边栏筛选

#### 4.2.2 账号分组
5 个预置分组：加密KOL组 50 / 科技KOL组 50 / AI KOL组 50 / 财经KOL组 30 / 网红博主组 20。

#### 4.2.3 批量操作
选中后顶部出现操作栏：
- 套用人格模板
- **随机生成人格**（从 20 条兴趣池 / 15 条性格池 / 10 条写作风格池中随机组合）
- 移入分组
- 删除

### 4.3 人格配置

#### 4.3.1 手动配置
字段：性别 / 国籍 / 年龄 / 兴趣爱好 / 性格特征 / 写作风格 / 个人简介。

#### 4.3.2 人格蒸馏
粘贴 5-20 条真实推文 → AI 提取人格特征并填充表单（当前为 Mock，后续对接 Claude API）。

#### 4.3.3 人格模板库
预置多套模板（加密分析师、科技博主等），一键套用。

### 4.4 信息源管理

支持 6 种类型：RSS / 网站 / Twitter / YouTube / Substack / Telegram。

预置库分 3 类：加密/Web3 · 科技 · AI/研究，共 12 个内置源。支持自定义 URL 添加。

### 4.5 自动发帖配置

| 配置项 | 可选值 |
|---|---|
| 发帖开关 | 开/关 |
| 发帖风格 | 随意/专业/幽默/思考型/大胆/学术 |
| 频率 | 每天 1/3/5 次 |
| 发帖时间 | 每个频率位一个具体时间点 |
| 活跃天数 | 周一 ~ 周日任选 |
| 发帖话题 / 避免话题 | 标签输入 |
| Hashtag / 表情符号 | 开/关 |

### 4.6 内容审核（草稿队列）

**入口**：一级导航"内容审核"，Dashboard 的"待审核草稿"统计卡片。

所有 AI 生成的推文进入"待审核"列表。每条可：
- ✅ **批准** —— 进入已批准状态，同步出现在内容日历中
- ✏️ **编辑** —— 内联修改后保存
- 🔄 **重新生成** —— AI 产出新版本
- ❌ **拒绝** —— 标记为拒绝

按状态筛选 tabs：全部 / 待审核 / 已批准 / 已拒绝。

### 4.7 内容日历

**周视图**：7 天 × 账号 × 时间点网格，显示整个矩阵的发帖排期。点击某一天 → 下方展开完整排期详情（时间 + 账号头像 + 话题 + 内容）。

**数据来源**：每个账号的 autopost 配置（activeDays × scheduledTimes）+ 已批准的草稿。

### 4.8 社交互动自动化 ⭐

让每个账号不只是发推，还会主动关注、转发、评论、回复 —— 看起来像真人而非机器。

#### 4.8.1 自动关注 Auto-Follow
- 规则：按关键词 / 按目标账号的粉丝 / 按话题标签
- 每日上限（防触发封号风控）

#### 4.8.2 自动转发 Auto-Retweet
- 触发条件：最小点赞数 + 白名单账号 + 关键词匹配
- **延迟随机化**：不立即转发，30 分钟~2 小时延迟模拟真人
- 可选 Quote Tweet（转发时带 AI 生成的评论）

#### 4.8.3 自动评论 Auto-Comment
- 目标账号列表
- 评论风格：支持型 / 提问型 / 补充观点型
- 模式：只评最新一条 / 随机选一条

#### 4.8.4 自动回复粉丝 Auto-Reply
- 触发类型：被 @ 提及 / 推文下评论 / 私信
- 规则：只回复关注者 / 白名单关键词
- 回复风格：感谢型 / 互动型 / 简短确认

#### 4.8.5 互动日志
账号详情页右侧展示近 7 天的所有自动互动事件（时间倒序）：
> "2 小时前 · 自动关注了 @BitMEXResearch"
> "3 小时前 · 转发了 @VitalikButerin 的推文"
> "5 小时前 · 评论了 @cz_binance："同意，链上数据印证了这点""
> "6 小时前 · 回复了粉丝 @john_doe 的评论"

**顶部统计卡片**：今日关注 / 今日转发 / 今日评论 / 今日回复 四项实时数据。

### 4.9 数据分析（per 账号）

**粉丝增长折线图**：近 30 天趋势，带渐变填充（SVG 纯手绘，数据由 accountId hash 确定性生成）。

**4 项互动统计**：粉丝增长 / 总点赞 / 总转发 / 总评论。

**最佳发帖时间热力图**：7 × 24 格，颜色深浅代表该时段互动率。

**Top 5 高互动推文**：按点赞/转发/评论综合排名。

### 4.10 账号健康评分

**0-100 综合健康分**，基于 4 个维度（各 25 分）：
- 发帖频率稳定性
- 互动率
- 内容一致性
- 风险信号

按分数分为 3 档：绿色健康（≥80）/ 黄色注意（60-79）/ 红色风险（<60）。

**展示位置**：
- 账号列表表格中显示紧凑彩色徽章
- 人格配置页右侧显示完整卡片（含各维度进度条 + 风险建议）

### 4.11 监控中心

#### 4.11.1 私信监控 + 4 类自动分类
- 🤝 合作邀约
- 💰 商务询价
- 🚨 垃圾信息
- 💬 普通消息

左侧列表按分类筛选，右侧详情展示完整内容与回复按钮。

#### 4.11.2 报警配置
支持 Lark（飞书） 和 Telegram 两种外部通道。配置 Webhook/Bot Token，设置触发条件（仅合作邀约 / 所有消息），支持测试发送。

### 4.12 通知中心

Sidebar 铃铛入口，点开弹出通知面板，时间倒序显示 5 类事件：
- 📝 发帖事件
- 💬 消息事件
- ⚠️ 账号健康异常
- ⚡ 系统操作（批量操作、草稿生成等）
- 🤝 互动事件（自动关注/转发/评论/回复）

未读消息有红色标记，支持"全部标为已读"。

### 4.13 全局搜索（Cmd+K 命令面板）

按 **⌘K** 或 **Ctrl+K** 弹出命令面板，模糊搜索：
- 6 个一级页面（直接跳转）
- 200 个账号（按 handle / displayName）
- 推文草稿（按内容 / 话题）
- 监控消息（按发件人 / 正文）

支持 ↑↓ 方向键导航、Enter 确认、ESC 关闭。

### 4.14 登录与身份

#### 4.14.1 登录页 `/login`
- 左侧品牌介绍（4 项核心价值点）
- 右侧登录按钮：GitHub / Google（内嵌官方 SVG 图标）+ "直接体验 Demo"
- Mock 登录：点击任意按钮 → 写入 localStorage → 跳转 Dashboard

#### 4.14.2 团队协作
Settings 页内显示团队成员列表（5 位 mock 成员，含头像/邮箱/角色/最后活跃时间）。角色：Owner / Admin / Editor / Viewer。"邀请成员"按钮占位。

#### 4.14.3 账户操作
- **重置 Demo 数据**：清除所有 localStorage 修改，恢复到初始 Mock
- **退出登录**：清除登录状态，返回登录页

### 4.15 设置

| 配置项 | 说明 |
|---|---|
| Claude API Key | 密码输入 |
| 模型选择 | Claude Sonnet / Haiku |
| 自动保存配置 | 开关 |
| 界面语言 | 中文 / English |
| 团队成员 | 列表 + 邀请 |
| 账户操作 | 重置 Demo / 退出登录 |

---

## 5. 视觉与交互规范

### 5.1 配色系统（OKX 白色亮色模式）

| 用途 | 颜色 |
|---|---|
| 页面背景 | `#F7F7F7` |
| 卡片/侧边栏 | `#FFFFFF` |
| 边框 | `#E8E8E8` |
| 主文字 | `#111111` |
| 次要文字 | `#999999` |
| 强调/按钮 | `#111111` |
| 成功色 | `#00BA7C` |
| 警告/危险 | `#E05252` |

### 5.2 头像方案
账号头像通过 `https://unavatar.io/twitter/{handle}` 拉取真实 Twitter 头像，失败时自动降级为 DiceBear 随机头像。

### 5.3 持久化
所有用户操作（随机人格、新增账号、批准草稿、互动配置等）通过 localStorage 伪持久化，刷新不丢失。设置页提供"重置 Demo 数据"按钮。

### 5.4 语言
默认中文，预留英文切换入口。

---

## 6. 产品边界（当前阶段不包含）

- ❌ 真实 Twitter API 接入
- ❌ 真实 Claude API 调用（所有 AI 功能为 Mock）
- ❌ 真实账号注册 / 防封号
- ❌ 数据库与用户系统（仅 localStorage）
- ❌ 深度数据分析与效果追踪
- ❌ 团队成员真实权限（仅 UI 展示）
- ❌ 订阅计费系统

---

## 7. 用户主流程

```
访问 smartkols.vercel.app
 ├── 未登录 → /dashboard (直接进入，或访问 /login 体验登录)
 └── 登录后 → /dashboard
       │
       ├── 查看概览统计 + 点击热门话题一键生成草稿
       │
       ├── 账号管理
       │    ├── 按分组筛选 / 搜索 / 查看健康分
       │    ├── 批量操作：套模板 / 随机人格 / 移分组 / 删除
       │    └── 进入账号详情（6 个子页）
       │          ├── 人格配置（手填 / 蒸馏 / 模板）+ 健康分
       │          ├── 信息源
       │          ├── 自动发帖
       │          ├── 互动自动化（关注/转发/评论/回复 + 日志）
       │          ├── 推文预览
       │          └── 数据分析（图表 + 热力图）
       │
       ├── 内容日历 → 看整个矩阵的周排期
       │
       ├── 内容审核 → 批准 / 编辑 / 拒绝 / 重新生成 AI 草稿
       │
       ├── 监控中心 → 处理合作邀约 + 配置 Lark/Telegram 报警
       │
       └── 设置 → API Key / 团队成员 / 重置数据 / 退出登录

全局：
 ├── Cmd+K 命令面板 快速跳转与搜索
 └── 通知中心 铃铛 查看最近事件
```

---

## 8. 技术栈

### 8.1 当前（Demo）
- **框架**：Next.js 14 App Router + TypeScript
- **样式**：Tailwind CSS（自定义 hex token）
- **UI**：自建 shadcn 风格组件 + Radix UI 原语
- **图标**：lucide-react + 内嵌 SVG（GitHub/Google 官方标识）
- **状态**：React Context (`MockStoreProvider`) + localStorage 持久化
- **数据**：`src/data/*.json`（accounts / groups / personas / persona-templates / autopost / sources / tweet-previews / monitoring / drafts / engagement-configs / engagement-logs / notifications）
- **部署**：Vercel（自动构建 + 自动部署）

### 8.2 后续集成方向
- Claude API（人格蒸馏 / 草稿生成 / 互动文案生成 / 消息分类）
- Twitter API（账号信息 / 发推 / 回复 / 关注 / 转发 / 评论 / 私信）
- 数据库（账号、配置、历史持久化）
- 外部通知（Lark Webhook / Telegram Bot）
- 用户认证与团队权限（真实 OAuth）

---

## 9. 开发阶段规划

| 阶段 | 目标 | 状态 |
|---|---|---|
| Phase 1 | 前端 Demo 基础：账号管理、人格、自动发帖、信息源、预览 | ✅ 已完成 |
| Phase 2 | 功能扩充：Dashboard、分组、批量操作、监控中心、报警、设置 | ✅ 已完成 |
| Phase 3 | 体验升级：白色模式、中文化、200 账号、真实头像、随机人格 | ✅ 已完成 |
| **Phase 4** | **完整叙事**：草稿队列、日历、数据分析、互动自动化、健康分、通知、Cmd+K、登录、团队、持久化、热门话题 | **✅ 已完成** |
| Phase 5 | AI 集成：对接 Claude API，实现真实人格蒸馏、草稿生成、互动文案生成 | ⏳ 待开始 |
| Phase 6 | Twitter 接入：API 对接，实现真实账号管理与发推 | ⏳ 待开始 |
| Phase 7 | 后端与持久化：数据库、用户系统、API 服务化 | ⏳ 待开始 |
| Phase 8 | 商业化：订阅计费、多用户权限、定制私有化 | ⏳ 待开始 |

---

## 10. Phase 4 Demo 特性汇总

### 11 项新增功能
- **A1 内容日历** —— 整个矩阵的周排期视图
- **A2 数据分析** —— 粉丝增长图、互动统计、7×24 热力图、Top 5 推文
- **A3 内容审核** —— AI 草稿批准/编辑/拒绝/重新生成工作流
- **A4 互动自动化** —— 自动关注/转发/评论/回复 + 近 7 天互动日志
- **B1 登录页** —— 品牌门面 + 三种登录方式（含 Mock）
- **B2 Cmd+K 命令面板** —— 全局模糊搜索与快速跳转
- **B3 账号健康评分** —— 4 维度综合评分 + 风险分级
- **B4 通知中心** —— 5 类事件聚合 + 未读标记
- **C1 热门话题探测** —— 10 条热门话题 + 一键生成草稿
- **C2 localStorage 持久化** —— 所有操作刷新不丢失
- **C3 团队协作** —— 成员列表 + 角色标签 + 邀请占位

### 关键数据规模
- **6 个一级模块** + **6 个账号子模块** = **12 个核心页面**
- **200 个 Mock 账号**跨 **5 个分组**
- **30 条预置草稿** · **50 条互动日志** · **15 条通知** · **10 条热门话题** · **5 位团队成员**
- 全局 **localStorage** 持久化，支持一键重置
